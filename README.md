# Menu_gotoxy
`Este archivo es un Menú de múltiples opciones que posee un límite de margen, si el gotoxy llega cerca del limite este para y cuando se le da enter se reinicia la consola para que no se salga del margen establecido, hecho con el IDE (Dev-C++) versión 2024 y un equipo de 15-16 pulgadas`
